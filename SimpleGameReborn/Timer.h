#pragma once
#include <iostream>
#include <thread>

void NormalTracker() {
	 //start = 20;
	/*while (start != 0) {
		std::this_thread::sleep_for(std::chrono::seconds(1));
		start--;
		if(start < 1)
			cout << start;
	}*/
	// Tracked! Attacker's IP has been sent to the FBI
}